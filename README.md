# everyDA
A opensource project to get data analtyics tools into the hands of mobile users for day to day personal use.

```
npm start
```

powershell.
```
$env:ANDROID_SDK_ROOT="$env:LOCALAPPDATA\Android\Sdk"
$env:Path += ";$env:ANDROID_SDK_ROOT\platform-tools;$env:ANDROID_SDK_ROOT\emulator"
adb devices
```